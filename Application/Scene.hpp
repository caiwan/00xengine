#ifndef SCENE_HPP
#define SCENE_HPP

#include "Engine.hpp"
/*
class GameScene : public engine::task::Scene{
public:
	GameScene(const std::string &pName);

	bool Start();
	void Stop();

	void Update();
	
	engine::Clock clk, fpsTimer;
	f32 elapsedTime;
	engine::Camera cam;
	engine::Text mousePos, fps;
	engine::Texture tex1;
	engine::Shader TextureShader, ColorShader, TextShader;
	engine::Light l;

	engine::Object obj1, obj2;
	engine::Mesh mesh1;

	bool movement;

	sf::RenderWindow* mWindow;

	engine::CRenderer* currRenderer;
};

class GameInput : public engine::task::Input{
public:
	GameInput(const std::string &pName);

	void SingleEvents();
	void RepetedEvents();
	
	int x, y;				// variables contenant la position de la souris
	int xClick, yClick;		// variables contenant l'endroit du clic droit

	GameScene* mScene;
};
*/
#endif
